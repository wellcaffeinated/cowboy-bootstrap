# Nomad Server Configuration
# Generated by Ansible

# Data directory
data_dir = "/opt/nomad"

# Datacenter
datacenter = "boulder"

#acl {
#  enabled = true
#}

# Advertise addresses on bootstrap LAN for netboot nodes
advertise {
  http = "{{ bootstrap_lan_ip }}"
  rpc = "{{ bootstrap_lan_ip }}"
  serf = "{{ bootstrap_lan_ip }}"
}

# Consul integration
consul {
  address = "127.0.0.1:8500"
  auto_advertise = true
  server_auto_join = true
  client_auto_join = true
}

# Vault integration
vault {
  enabled = true
  address = "http://127.0.0.1:8200"
  create_from_role = "nomad-cluster"
}